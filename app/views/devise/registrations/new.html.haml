.devise
  .devise__wrapper
    = render "devise/template/register_header_1"
    .sign-up
      .sign-up__top
        会員情報入力
      .sign-up__bottom
        = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
          = render "devise/shared/error_messages", resource: resource
          .field
            = f.label :ニックネーム 
            %span 必須
            %br/
            = f.text_field  :name, autofocus: true, placeholder: "例）フリマ太郎"
          .field
            = f.label :メールアドレス
            %span 必須
            %br/
            = f.email_field :email, autofocus: true, placeholder: "PC・携帯どちらでも可"
          .field
            = f.label :パスワード
            %span 必須
            %br/
            %p ※ 英字と数字の両方を含めて設定してください
            = f.password_field :password, placeholder: "7文字以上の半角英数字"
            %br/
            = f.password_field :password_confirmation, placeholder: "確認のためもう一度入力してください"
          
          .identify
            %h3 本人確認
            %p 安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          .field
            %label お名前(全角)
            %span 必須
            %br/
            .two_rows_input
              %input{name: "family_name", placeholder: "例）山田"}
              %input{name: "family_name", placeholder: "例）彩"}
          .field
            %label お名前カナ(全角)
            %span 必須
            %br/
            .two_rows_input
              %input{name: "family_name_kana", placeholder: "例）ヤマダ"}
              %input{name: "family_name_kana", placeholder: "例）アヤ"}
          .field.field--birthday
            %label 生年月日
            %span 必須
            %br/
            .birthday_input
              %select{name: "birth_year"}
                %option --
                %option 2020
              %span 年
              %select{name: "birth_month"}
                %option --
                %option 1
              %span 月
              %select{name: "birth_date"}
                %option --
                %option 1
              %span 日
            %p ※ 本人情報は正しく入力してください。会員登録後、
            %p 修正するにはお時間を頂く場合があります。

          .confirm_message
            「次へ進む」のボタンを押すことにより、
            %span 利用規約
            に同意したものとみなします
          = link_to "/addresses", class:"send_btn" do
            次へ進む
          .about_message
            本人情報の登録について＞

  = render "devise/template/footer"